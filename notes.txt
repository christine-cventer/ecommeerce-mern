/** Controllers/ProductCategory creates a categories.
 Only an admin can create new categories (change role=1 in db)
 See route routes/CreateProductCategoryRoute

 Controllers/Product uploads a new image and refs back to ProductCategory via ObjectId
 See routes/createNewProductRoute 

configs/multerConfig uses multer to parse form-data from image


 ***Should I move some of this configuration over to the Product controller? 
 *** Particularly when I have to create a req.obj for the buffer variable? ***

**/
